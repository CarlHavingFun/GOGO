# CODEX_START｜GOGO 开发执行入口

> 本文件是 Codex、其他代码 Agent 与新开发者进入仓库后的第一阅读入口。
>
> 不要从“把全部设计一次实现”开始；必须按可玩闭环、可验证结果和阶段门槛推进。

---

## 1. 项目一句话

《GOGO》是一款 Godot 4.x 制作的俯视角手动瞄准动作肉鸽：玩家选择一名拥有正负天赋的原创电竞角色，携带一把主武器和两个投掷物槽，在固定竞技场中完成 20 波生存，通过枪械操作、敌人职责、受控随机升级和角色怪癖形成每局不同的构筑。

## 2. 最高优先级

开发取舍顺序固定为：

1. 操作可读性；
2. 射击手感与命中反馈；
3. 构筑选择质量；
4. 角色正负天赋的可玩性；
5. 公平且可解释的失败；
6. 性能与可维护性；
7. 内容数量；
8. 电竞梗还原程度。

任何新增功能如果不能服务前六项之一，应暂缓实现。

---

## 3. 唯一设计真相来源

权威设计目录为：

```text
docs/design/
```

阅读顺序：

1. `README_设计文档索引.md`；
2. `00_产品宪法.md`；
3. 当前任务对应的专项文档；
4. `09_Godot技术架构.md`；
5. `10_平衡测试规范.md`；
6. `11_开发里程碑.md`；
7. `12_游戏性与素材Review.md`；
8. `13_素材生产管线与提示词.md`；
9. `14_像素美术Style_Bible.md`。

地图或刷怪任务额外必读：

1. `06_怪物与波次设计.md`；
2. `15_竞技场与刷怪空间系统.md`；
3. 对应的 `arenas/15X_*.md` 内容附录。

发生冲突时：

- `00_产品宪法.md` 优先；
- 后编号专项文档中的明确规则优先于前文概述；
- 已记录版本的平衡调整优先于旧初始值；
- 临时代码行为不自动成为设计规则。

禁止重新创建另一套平行 GDD、`docs/00_*.md`、独立地图包或内容重复的总结文档。

---

## 4. 技术边界

- 引擎：Godot 4.x 稳定版；
- 首发平台：Windows；
- 类型：2D 俯视角单人 PvE；
- 输入：WASD 移动、鼠标瞄准、左键射击；
- 武器：单主武器制；
- 弹药：无限备弹，保留弹匣和换弹节奏；
- 判定：优先使用射线命中与可见曳光；
- 地图：第一版单个固定竞技场，不做多房间探索；
- 首发地图：双尘旧城；赤瓦小镇和货运枢纽只作为内容储备；
- 生成：外围缓冲区、屏幕外生成、安全距离、入口容量和无合法点延后是硬规则；
- 数据：角色、武器、升级、敌人、波次和 Arena 定义应数据驱动；
- 随机：使用可注入种子和独立随机子流；
- 资产：角色、武器、投掷物、子弹、特效、UI 和场景模块必须独立组合。

第一版不做：多人、PvP、随机迷宫、皮肤交易、复杂背包、多主武器自动攻击、真实职业选手肖像与队伍素材。

---

## 5. 推荐仓库结构

目录只在第一个真实文件落地时创建，不提前建立空壳。

```text
GOGO/
├── project.godot
├── README.md
├── CODEX_START.md
├── docs/
│   ├── design/
│   │   └── arenas/
│   └── superpowers/
│       ├── specs/
│       └── plans/
├── assets/
│   ├── characters/
│   ├── weapons/
│   ├── throwables/
│   ├── enemies/
│   ├── projectiles/
│   ├── effects/
│   ├── arena/
│   ├── ui/
│   └── audio/
├── data/
│   ├── characters/
│   ├── weapons/
│   ├── upgrades/
│   ├── enemies/
│   ├── waves/
│   └── arenas/
├── arenas/
│   ├── common/
│   ├── m0/
│   └── double_dust/
├── systems/
│   ├── combat/
│   └── spawn/
├── entities/
├── run/
├── ui/
├── vfx/
└── tests/
    ├── unit/
    ├── integration/
    ├── golden_seeds/
    └── performance/
```

在赤瓦小镇或货运枢纽正式立项前，不创建其代码或正式素材空目录。

---

## 6. 开发原则

### 6.1 一次只实现一个闭环

每个任务必须同时具备：

- 玩家可观察的结果；
- 明确允许修改的目录；
- 明确不改的范围；
- 可重复执行的验收步骤；
- 失败日志或调试信息；
- 对应设计章节引用。

禁止任务：

> “实现完整游戏”或“同时完成三张地图”。

合格任务：

> “把 M0 的矩形竞技场抽成可替换 Arena 子场景，由 Arena 注入玩家活动边界，同时保持现有射击、假人和自动测试行为不变。”

### 6.2 数据与逻辑分离

应数据化：

- 武器基础数值；
- 角色属性与标签；
- 升级定义、稀有度、前置和冲突；
- 敌人预算、职责和基础属性；
- `WaveSpawnProfile` 的预算、权重、配额和批次间隔；
- `ArenaDefinition` 的地图身份、场景路径、标签和性能配置；
- `SpawnPortal` 的入口类型、容量、支持标签和冷却；
- 素材路径和导入元数据。

应保留在代码中：

- 状态机；
- 伤害结算顺序；
- 射击与后坐力算法；
- 奖励池过滤算法；
- 入口硬过滤、评分和无合法点延后规则；
- 存档迁移；
- 遥测采集。

禁止在 `SpawnDirector` 中根据具体地图 ID 写条件分支。

### 6.3 先灰盒，后正式素材

在玩法通过前：

- 允许使用几何图形和临时音效；
- 不允许因为等待正式素材而阻塞射击、升级、波次或刷怪验证；
- 正式素材必须遵守 `13_素材生产管线与提示词.md` 和 `14_像素美术Style_Bible.md`；
- 完整竞技场概念图只能作为布局参考；
- 不把 AI 输出大图直接作为最终地图；
- 地面、墙体、入口、掩体、Decal 和环境动画分别生产。

---

## 7. 里程碑执行顺序

### M0｜AK 射击玩具

只验证：

- 玩家移动；
- 鼠标瞄准；
- AK 射击；
- 后坐力与准星恢复；
- 弹匣与换弹；
- 静止假人；
- 命中、击杀和调试反馈；
- 固定种子散布。

M0 不做完整敌人 AI、升级、商店、角色天赋、波次或正式地图。

M0 后置工程任务允许把竞技场抽成可替换 Arena，但不得顺手加入刷怪和三张地图。

### M1｜五波闭环与双尘旧城灰盒

增加：

- 双尘旧城灰盒；
- 外围出生缓冲网络；
- 8 个普通入口、2 个特殊入口占位和 1 个 Boss 入口占位；
- 近战追击怪、快速怪、远程预警怪；
- `WaveDirector`、`SpawnDirector` 和固定种子入口选择；
- Wave 1～5；
- 赏金、等级、三选一、死亡和重新开始；
- 12～15 个基础升级；
- 入口使用率、拥堵和寻路日志。

M1 门槛：新玩家能够完成一次五波闭环、说明死亡原因，并愿意重开测试另一条构筑。

### M2｜角色与战术验证

增加尼尼、大表哥、Deagle 或 AWP、烟雾、背身诱导怪和至少 20 个升级。

验证：

1. 角色负面天赋可预警、可规避、可缓解、可反转；
2. 烟雾不破坏敌人、入口预警和地面危险区可读性。

不增加第二张正式地图。

### M3｜十波垂直切片

增加完整战斗骨架、商店、第二投掷物槽、小 Boss、固定种子回归、双尘旧城首轮正式像素素材和第一版正式 HUD。

### M4｜二十波 Alpha

扩展到五角色、五枪、四投掷物、47 个升级、完整敌人职责、20 波和最终 Boss。

### M5｜内容完成与平衡

执行全组合测试、难度 3～5、经济、Boss、教程、可访问性、性能、原创化和黄金种子回归。

### M6｜Windows Beta

完成正式美术音频、菜单设置、手柄、多分辨率、安装更新、崩溃日志、本地化框架和外部测试。

### M7｜Windows 1.0

完成发布构建、法务和素材来源清单、存档安全、支持信息和发布门槛。

### M8｜Web 与移动适配

仅在 Windows 核心稳定后立项，重新设计触摸输入、辅助瞄准、UI 安全区和移动性能档。

---

## 8. 地图或刷怪 `/goal` 模板

```text
/goal
在当前 GOGO 仓库中完成一个地图或刷怪闭环。

开始前：
1. 阅读 CODEX_START.md；
2. 阅读 docs/design/00_产品宪法.md；
3. 阅读 docs/design/06_怪物与波次设计.md；
4. 阅读 docs/design/09_Godot技术架构.md；
5. 阅读 docs/design/11_开发里程碑.md 中的当前阶段；
6. 阅读 docs/design/15_竞技场与刷怪空间系统.md；
7. 阅读当前地图对应的 docs/design/arenas/15X_*.md。

约束：
- 不实现未进入当前里程碑的地图；
- 不在 SpawnDirector 中写具体地图 ID 特例；
- 地图先灰盒、缓冲区、入口和导航，后正式素材；
- 生成原点不可进入玩家镜头；
- 无合法入口时延后，不贴脸生成；
- 固定种子必须重现入口选择；
- 给出入口使用率、拥堵、寻路失败和性能证据。

最终汇报：
- 修改文件；
- 设计章节；
- 运行和测试命令；
- 实际结果；
- 未验证项和已知风险。
```

---

## 9. 每个任务的完成定义

Codex 在声称完成前必须：

1. 运行项目或对应无头测试；
2. 记录实际执行命令与结果；
3. 检查解析错误、运行错误和新增警告；
4. 完成手动可观察验收；
5. 检查是否误改无关文件；
6. 更新相关数据或文档；
7. 汇报仍未验证的部分，不得用推测替代证据。

仅“代码已写完”不算完成。

---

## 10. 素材任务的完成定义

每个正式素材必须同时具备：

- 唯一 `asset_id`；
- 目标路径；
- 类别与用途；
- 逻辑尺寸和画布尺寸；
- 锚点与朝向；
- 动画帧数与 FPS；
- 透明背景要求；
- Prompt 与负面约束；
- 原始生成文件；
- 清理后的最终 PNG；
- Godot 导入截图或验收记录；
- 资产状态：`planned/generated/cleaned/approved/in_game/rejected`。

人物、枪械、投掷物、特效和场景模块禁止画死在同一张最终资产中。

---

## 11. 当前立即动作

1. 完成像素 Style Bible 评审；
2. 合并第 15 号竞技场与刷怪空间文档；
3. 单独建立 M0 Arena 抽离工作包，保持射击测试不变；
4. 再进入 M1 双尘旧城灰盒、三种敌人和五波闭环；
5. 只生产当前里程碑和双尘旧城需要的场景资产。

在双尘旧城和五波闭环通过前，不实现赤瓦小镇、货运枢纽或三地图轮换。
