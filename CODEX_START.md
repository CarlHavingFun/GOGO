# CODEX_START｜GOGO 开发执行入口

> 本文件是 Codex、其他代码 Agent 与新开发者进入仓库后的第一阅读入口。
>
> 不要从“把全部设计一次实现”开始；必须按可玩闭环、可验证结果和阶段门槛推进。

---

## 1. 项目一句话

《GOGO》是一款 Godot 4.x 制作的俯视角手动瞄准动作肉鸽：玩家选择一名拥有正负天赋的原创电竞角色，携带一把主武器和两个投掷物槽，在固定 Seed 可复现、按 Chunk 流式延伸的无尽黄沙旧城战场中完成 20 波生存，通过枪械操作、敌人职责、受控随机升级和角色怪癖形成每局不同的构筑。

---

## 2. 最高优先级

开发取舍顺序固定为：

1. 操作可读性；
2. 射击手感与命中反馈；
3. 构筑选择质量；
4. 角色正负天赋的可玩性；
5. 公平且可解释的失败；
6. 性能与可维护性；
7. 内容数量；
8. 电竞梗还原程度。

任何新增功能如果不能服务前六项之一，应暂缓实现。

---

## 3. 唯一设计真相来源

权威设计目录为：

```text
docs/design/
```

基础阅读顺序：

1. `README_设计文档索引.md`；
2. `00_产品宪法.md`；
3. 当前任务对应的专项文档；
4. `09_Godot技术架构.md`；
5. `10_平衡测试规范.md`；
6. `11_开发里程碑.md`；
7. `12_游戏性与素材Review.md`；
8. `13_素材生产管线与提示词.md`；
9. `14_像素美术Style_Bible.md`。

无限地图、战斗空间或刷怪任务额外必读：

1. `06_怪物与波次设计.md`；
2. `15_竞技场与刷怪空间系统.md`；
3. `16_无尽双尘荒原与程序化模块系统.md`。

发生冲突时：

- `00_产品宪法.md` 优先；
- 后编号专项文档中的明确规则优先于前文概述；
- `16_无尽双尘荒原与程序化模块系统.md` 覆盖旧文档中“第一版固定竞技场”的描述；
- 已记录版本的平衡调整优先于旧初始值；
- 临时代码行为不自动成为设计规则。

禁止重新创建另一套平行 GDD、`docs/00_*.md`、独立地图包或内容重复的总结文档。

---

## 4. 当前可运行状态

### 4.1 M0 射击回归场景

```text
run/m0_shooting_toy.tscn
```

保留并验证：

- WASD 移动；
- 鼠标手动瞄准；
- AK 射线射击；
- 固定 Seed 散布；
- 后坐力和准星恢复；
- 30 发弹匣、手动/自动换弹；
- 静止假人、伤害、击杀和反馈。

禁止为了无限地图功能破坏该场景及其测试。

### 4.2 默认无限荒漠场景

```text
run/infinite_desert_prototype.tscn
```

当前已经实现：

- `1024×1024 px` Chunk；
- 玩家周围半径 `2`，即 `5×5` 期望集合；
- 每帧最多创建一个 Chunk；
- 前进方向优先加载；
- 离开范围后回收；
- 世界 Seed `424242`；
- 六种普通灰盒战斗模块；
- 稀疏 `supply_outpost` POI；
- Terrain 碰撞；
- Camera2D 跟随；
- 玩家屏幕外 `850～1250 px` 环形刷怪；
- 最大 24 个活动灰盒敌人；
- 预热 8 个对象池敌人；
- 超过 `2100 px` 或被击杀后回收。

当前默认 `project.godot` 启动无限荒漠场景。

---

## 5. 技术边界

- 引擎：Godot `4.7.1 stable`；
- 首发平台：Windows；
- 类型：2D 俯视角单人 PvE；
- 输入：WASD 移动、鼠标瞄准、左键射击、R 换弹、T 重置武器散布 Seed；
- 武器：单主武器制；
- 弹药：无限备弹，保留弹匣和换弹节奏；
- 判定：射线命中与可见曳光；
- 默认地图：单一黄沙旧城主题的流式无限世界；
- 随机：地图、敌人和武器散布使用独立随机子流；
- 生成：屏幕外、安全距离、已加载 Chunk、Terrain 过滤和无合法点延后是硬规则；
- 资产：角色、武器、投掷物、子弹、特效、UI、地面、墙体、掩体和 POI 必须独立组合；
- 第一版核心不依赖 WFC、Gaea、YATI 或第三方无限地形运行时插件。

第一版不做：多人、PvP、随机迷宫、复杂背包、多主武器自动攻击、完整城镇、地下迷宫、可破坏建筑、永久世界存档、真实职业选手肖像与队伍素材。

---

## 6. 当前代码边界

```text
systems/world/desert_chunk_layout.gd
```

纯逻辑：世界 Seed 和 Chunk 坐标到模块、旋转、POI 与装饰 Seed。不得访问场景树和全局 RNG。

```text
systems/world/chunk_stream_planner.gd
```

纯逻辑：负坐标映射、期望坐标、加载优先级和卸载列表。

```text
arenas/infinite_desert/infinite_desert_chunk.gd
```

单个 Chunk 的灰盒绘制与 Terrain 碰撞。不得决定波次和敌人组成。

```text
systems/world/infinite_chunk_manager.gd
```

负责 Chunk 生命周期、加载队列和活动坐标。不得决定敌人类型和波次预算。

```text
systems/spawn/spawn_ring_sampler.gd
```

纯逻辑：固定 Seed 的环形候选采样。

```text
systems/spawn/infinite_spawn_director.gd
```

负责候选过滤、活动敌人上限和对象池。不得根据具体 `module_id` 编写地图特例。

```text
entities/enemies/prototype_chaser.gd
```

只用于验证追击、Terrain 碰撞、AK 命中和回收链路；不是正式敌人设计。

---

## 7. 开发原则

### 7.1 一次只实现一个闭环

每个任务必须同时具备：

- 玩家可观察的结果；
- 明确允许修改的目录；
- 明确不改的范围；
- 可重复执行的验收步骤；
- 失败日志或调试信息；
- 对应设计章节引用。

禁止任务：

> “实现完整游戏”“一次完成五角色和二十波”或“同时完成三套地图生态”。

合格任务：

> “在现有无限荒漠场景中实现 Wave 1～3：复用环形刷怪和 Chunk 过滤，加入三种敌人和固定 Seed 编队，并保持 M0 与无限地图回归测试通过。”

### 7.2 数据与逻辑分离

应数据化：

- 武器基础数值；
- 角色属性与标签；
- 升级定义、稀有度、前置和冲突；
- 敌人预算、职责和基础属性；
- 波次预算、权重、配额和批次间隔；
- 世界 Seed、Chunk 尺寸、活动半径和模块权重；
- POI 密度与最小间隔；
- 素材路径和导入元数据。

应保留在代码中：

- 状态机；
- 伤害结算顺序；
- 射击与后坐力算法；
- Chunk 坐标与生命周期；
- 候选硬过滤和无合法点延后；
- 奖励池过滤算法；
- 存档迁移；
- 遥测采集。

### 7.3 先灰盒，后正式素材

在无限移动、碰撞、刷怪和性能通过前：

- 允许使用程序绘制矩形、圆形和临时颜色；
- 不允许因为等待正式素材而阻塞玩法验证；
- 不把 AI 输出大图直接作为最终地图；
- 地面、墙体、入口、掩体、Decal、POI 和环境动画分别生产；
- 正式素材必须遵守 `13_素材生产管线与提示词.md` 和 `14_像素美术Style_Bible.md`。

---

## 8. 当前执行顺序

1. **M0 射击底座**：已实现并作为回归基线保留；
2. **E0 无限移动底座**：Chunk 加载、回收和 Camera2D 已实现；
3. **E1 确定性战斗模块**：六种模块、旋转、Terrain 和补给站 POI 已实现；
4. **E2 动态刷怪链路**：环形候选、过滤、追击敌人和对象池已实现；
5. **当前人工门槛**：Windows 图形模式连续移动和射击十分钟；
6. **M1 五波闭环**：三种正式灰盒敌人、赏金、等级、三选一、死亡和重新开始；
7. **M2 角色战术验证**：尼尼、大表哥、烟雾和负面天赋；
8. **M3 十波垂直切片**：正式像素地图模块、HUD、小 Boss 和固定种子回归；
9. 通过门槛后扩展完整五角色、五枪、47 升级和 20 波；
10. Windows 1.0 后评估固定竞技场模式、赤瓦小镇、货运枢纽和跨平台适配。

---

## 9. 验证命令

### 9.1 解析与导入

```bash
godot --headless --path . --editor --quit
```

### 9.2 完整自动测试

```bash
godot --headless --path . res://tests/test_runner.tscn
```

当前测试覆盖：

- 武器状态和换弹合同；
- 固定 Seed 武器散布；
- Chunk 布局确定性和负坐标；
- 模块多样性和稀疏 POI；
- `5×5` 期望集合、前方优先和卸载；
- 环形候选确定性和距离边界；
- M0 场景结构；
- 无限荒漠场景结构与碰撞掩码。

### 9.3 默认主场景 smoke

```bash
godot --headless --path . --quit-after 600
```

### 9.4 Windows 人工验收

至少连续运行十分钟并检查：

- 跨越正负 Chunk 坐标无明显地图空洞；
- 玩家不会被错误卡在 Chunk 接缝；
- 同一坐标重新进入后模块一致；
- 活跃 Chunk 数不会持续增长；
- 敌人不会在屏幕内或墙体中生成；
- 敌人被击杀或过远后能回收；
- AK 射线会被 Terrain 阻挡；
- FPS、内存和操作手感可接受；
- 控制台无新增脚本错误。

---

## 10. 无限地图 `/goal` 模板

```text
/goal
在当前 GOGO 仓库中完成一个无限地图或刷怪闭环。

开始前：
1. 阅读 CODEX_START.md；
2. 阅读 docs/design/00_产品宪法.md；
3. 阅读 docs/design/06_怪物与波次设计.md；
4. 阅读 docs/design/09_Godot技术架构.md；
5. 阅读 docs/design/10_平衡测试规范.md；
6. 阅读 docs/design/16_无尽双尘荒原与程序化模块系统.md。

约束：
- 保持 run/m0_shooting_toy.tscn 回归通过；
- 不在 Chunk 中决定波次；
- 不在 SpawnDirector 中写 module_id 特例；
- 地图先灰盒和性能验证，后正式素材；
- 生成点必须在屏幕外、已加载 Chunk 内且不与 Terrain 重叠；
- 无合法点时延后，不贴脸生成；
- 固定 Seed 必须重现地图和候选序列；
- 地图、敌人和武器 RNG 必须彼此独立；
- 必须给出自动测试和实际运行证据。

最终汇报：
- 修改文件；
- 对应设计章节；
- 运行和测试命令；
- 实际结果；
- 未验证项和已知风险。
```

---

## 11. 每个任务的完成定义

Codex 在声称完成前必须：

1. 先写失败测试并确认失败原因正确；
2. 实现最小生产代码；
3. 运行项目解析和完整自动测试；
4. 运行相关场景 smoke；
5. 检查解析错误、运行错误和新增警告；
6. 完成任务要求的手动可观察验收，无法执行时明确列为未验证；
7. 检查是否误改无关文件；
8. 更新相关设计、数据或开发入口；
9. 汇报实际命令、结果、分支、提交和 CI 状态。

“代码已写完”不等于完成；必须有新鲜的验证证据。
