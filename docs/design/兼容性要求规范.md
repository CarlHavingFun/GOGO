使用 GDScript，不使用 C# 作为主要脚本语言。
选择 Compatibility Renderer。
所有操作使用 Input Map，不直接绑定具体设备。
UI 使用 Control、Container、Anchor，不用固定屏幕坐标硬摆。
统一使用 res:// 和 user://，不使用 Windows 绝对路径。
避免依赖只支持 Windows 的原生插件和 DLL。
素材文件名统一用小写英文，并严格保持大小写一致。